# MCP Python SDK direct security-policy bundle

This bundle is an offline capture of the public `modelcontextprotocol/python-sdk` security policy retrieved on 2026-08-09 from an immutable GitHub commit. It contains one verbatim `SECURITY.md` with:

- an explicit Supported Versions table;
- a private GitHub Security Advisory reporting route;
- a prohibition on public issue, discussion, or pull-request disclosure; and
- the requested minimum report contents.

The supported-version rows are executable derivation evidence: two rows receive security fixes and one row is explicitly unsupported. The compiler must reproduce that 2/1 split exactly. This bundle grants no live authority, performs no network I/O during compilation, and must not be used to test the SDK or any deployed service.
