# Codex activity - GreyTheory navigation scrollbar and keyboard

## Scope

Correct the mismatched navigation scrollbar, retain the selected visual system,
and continue the Windows learner-pilot acceptance roadmap without changing
security posture.

## Actions

- Added shared scrollbar tokens and a visible, stable navigation rail.
- Added focused regression coverage for scrollbar visibility and palette.
- Captured and inspected current 390-pixel closed/open drawer states and the
  1440-pixel desktop shell.
- Reconfirmed drawer reverse/forward focus wrap, Escape close, trigger return,
  and a logical runtime focusable inventory beginning with Skip to workspace.
- Attempted the requested Figma evidence update after inspection; stopped when
  the Starter-plan MCP limit refused the atomic write.
- Synchronized capability truth, QA, changelog, roadmap, logs, and history.

## Guardrails retained

`LOCAL_FIXTURE` remains the ceiling. No external request, account connector,
credential, worker, Ubuntu service, VPS, publication, or posture decision was
activated.

## Handoff

Use a browser harness that can originate Tab from an unfocused body to complete
the first-entry sweep, then perform the separate-user Windows shortcut or
installer persistence/recovery gate.
