# GreyTheory release media and persisted-flow acceptance

## Repo-truth delta

The current Guided Mission Control surfaces now have accepted release-media
captures. The real same-origin local application was exercised in the in-app
browser: a bounded learning assignment persisted across a full reload, and
route navigation moved focus into the selected workspace. The product remains
`LOCAL_FIXTURE`; full sequential-keyboard, clean-user Windows package, Ubuntu
worker, and live-programme acceptance remain open.

## Built

- Current repository media for Mission Control, the focused learning path, the
  beginner-to-transfer topic roadmap, and contract-only public intelligence.
- Route-level workspace focus management with visible focus and
  `prefers-reduced-motion` support.
- Static accessibility contracts covering route focus, skip navigation,
  current-page semantics, and modal focus/Escape behavior.
- A ChaseInTech-ready evidence set and release-media audit under the central
  project-specific visual-QA roots.

## Verification

- Same-origin browser: service connected with `APP CONNECTED`, one assigned
  IDOR/BOLA explain step persisted after full reload, and Intelligence route
  focus landed on `main#workspace-main`.
- In-app browser: zero console errors and zero desktop document overflow.
- `npm run test` - 16 UI tests passed.
- `npm run test:sites` - 4 Sites tests passed.
- `npm run build` - passed and prepared the Sites bundle.
- `python -m pytest tests/test_local_workbench_transport.py tests/test_intelligence_contract.py -q` - 17 passed.

## Boundaries retained

No target contact, provider fetch, credential use, posture change, Ubuntu
activation, programme connection, submission, merge, or production deployment
occurred as part of this GreyTheory repository change.

## Next safe action

Finish sequential-keyboard acceptance, then package and exercise the local
application under a clean Windows user. Only after that should governed coach
conversation and the two queued Case Packs advance.
